jQuery(function() {
    // TO DO:
    // 1. Check if there are placeholders
    // 2. If yes, then load the same page with full_render=1 query parameter again by Ajax
    // 3. Onload parse the HTML as jQuery object
    // 4. For each placeholder in that jQuery object, populate the placeholder in the document with placeholder's content
    // 5. Trigger a special event "all_loaded"
});